{
    "name": "@uk/cli",
    "version": "2.0.0",
    "main": "dist/index",
    "types": "dist/index.d.ts",
    "author": "koloboid",
    "license": "MIT",
    "scripts": {
        "clean": "rm -fr dist node_modules",
        "compile": "cd src && tsc",
        "build": "yarn clean && yarn && yarn compile && yarn lint",
        "lint": "eslint --ext .ts,.tsx src/**/*"
    },
    "devDependencies": {
        "@types/node": "*",
        "typescript": "^4.3.2"
    }
}
